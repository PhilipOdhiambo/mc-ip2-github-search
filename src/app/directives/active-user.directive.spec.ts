import { ActiveUserDirective } from './active-user.directive';

describe('ActiveUserDirective', () => {
  it('should create an instance', () => {
    const directive = new ActiveUserDirective();
    expect(directive).toBeTruthy();
  });
});
